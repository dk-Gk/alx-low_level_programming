#include "main.h"
/**
 * int _islower(int c) - check alphabet case
 * Return: 1 if true and 0 if not
 */
int int _islower(int c);
{
char b;
for (b = 'a'; b <= 'z'; b++)
{
if (c = b)
{
return (1);
}
else
{
return (0);
}
}
}
