#include "main.h"
/**
 * puts2 - function that prints every other character of a string
 * @s: pointer value
 * Return: nothing
 */
void puts2(char *str)
{
int c = 0, i;
while (s[c])
{
c = c + 1;
}
_putchar(s[0]);
for (i = 1; i <= c - 2; i++)
{
_putchar(s[i + 2]);
}
_putchar('\n');
}
